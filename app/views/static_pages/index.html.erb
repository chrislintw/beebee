<section class="jumbotron text-center">
  <div class="container">
    <h1 class="jumbotron-heading">BeeeBee</h1>
    <p class="lead text-muted">Something short and leading about the collection below—its contents, the creator, etc. Make it short and sweet, but not too short so folks don't simply skip over it entirely.</p>
    <p>
      <%= link_to 'Primium Plan', profile_path, class: 'btn btn-info my-2' %>
      <a href="#" class="btn btn-secondary my-2">Join Us</a>
    </p>
  </div>
</section>
<% @products.in_groups_of(3, false) do |group| %>
<div class="row">
  <% group.each do |product| %>
  <div class="col-md-4 shadow-sm">
    <div class="card mb-4 product-image">
      <% if product.image.attached? %>
        <%= image_tag url_for(product.image) %>
      <% else %>
      <img class="" data-src="holder.js/100px225?theme=thumb&bg=55595c&fg=eceeef&text=<%= product.name %>" alt="Card image cap">
      <% end %>
      <div class="card-body">
        <h5 class="card-title"><%= product.name %></h5>
        <div class="d-flex justify-content-between align-items-center">
          <div class="btn-group">
            <%= link_to 'Buy', product_path(product), class: 'btn btn-sm btn-info ' %>
          </div>
          <small class="text-muted"><%= display_price product %></small>
        </div>
      </div>
    </div>
  </div>
  <% end %>
</div>
<% end %>

